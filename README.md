# Glance

Glance is a macOS menu bar utility that shows a minimal camera preview window instantly.

## Privacy

- No network calls.
- No analytics or telemetry.
- Camera is active only while the preview window is visible.
- Camera is stopped when the window is closed or the app is idle.

